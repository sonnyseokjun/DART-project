# 환경 변수 (API 키 등 비밀 값)
.env

# Python
__pycache__/
*.py[cod]
venv/

# Django
db.sqlite3
/staticfiles/

# 테스트 커버리지 (측정할 때마다 새로 생기는 산출물)
.coverage
htmlcov/

# 단계 전환 시 _workspace/ 를 복사해 두는 로컬 아카이브.
# 원본은 이미 git 이력에 있으므로 사본까지 추적하지 않는다.
_workspace_*/

# IDE / OS
.vscode/
.idea/
Thumbs.db
