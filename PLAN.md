# DART 공시 쉬운 요약 웹서비스 — 프로젝트 기획서 (초안)

> **문서 버전** v0.3 (초안) · **작성일** 2026-07-23 · **작성자** seokjun
> **변경 이력**
> · v0.2 — 신규 공시 준실시간 자동 반영 설계(4.4) 추가
> · v0.3 — 준실시간화 심화 전략(4.5), 비용 구조(11), 확장성·API 한도 대응(12) 추가 및 아키텍처를 확장성 고려해 보강
> **상태** 기획 단계 — 검토 및 피드백 후 확정 예정

> 📐 **설계 원칙 (확장성 관점)**: 이 서비스의 핵심 설계 결정은 **"사용자 요청과 DART API 호출을 완전히 분리"** 하는 것이다. 사용자는 항상 우리 DB/캐시에서 읽고, DART API는 오직 백엔드 수집 파이프라인만 호출한다. 그 결과 **DART 호출 수는 사용자 트래픽과 무관**하며(사용자 확장 ≠ API 부하), 트래픽 확장은 웹/DB 계층에서, API 한도는 수집 계층에서 각각 독립적으로 해결한다. 이 원칙이 문서 전반의 아키텍처를 관통한다.

---

## 1. 프로젝트 개요

### 1.1 한 줄 소개

DART 전자공시 시스템에 올라오는 기업 공시를, 경제·회계 지식이 없는 일반인도 이해할 수 있도록 AI가 쉬운 언어로 요약·해설해 주는 웹서비스.

### 1.2 배경 및 문제 정의

금융감독원 전자공시시스템(DART)은 상장기업의 모든 공시를 투명하게 공개하지만, 공시 원문은 회계·법률 용어와 표로 가득해 일반 투자자가 읽고 해석하기 매우 어렵다. "유상증자 결정", "타법인 주식 취득", "단일판매·공급계약 체결" 같은 제목만 보고는 그것이 주가나 기업 가치에 어떤 의미인지 판단하기 힘들다.

이 프로젝트는 **원문 공시 → AI 요약·해설 → 일반인 친화적 카드 형태**로 변환하여, 사용자가 관심 기업의 공시 흐름을 빠르고 쉽게 파악하도록 돕는다.

### 1.3 핵심 가치 제안 (Value Proposition)

- **쉬운 언어**: 전문 용어를 풀어쓰고, "이게 왜 중요한가"를 한 문장으로 설명
- **섹터 단위 탐색**: 개별 기업뿐 아니라 산업 섹터별로 묶어 흐름을 비교
- **신뢰성**: 모든 요약에 원문 공시 링크를 병기하여 출처를 검증 가능하게 함

### 1.4 1차 목표 범위 (MVP Scope)

- 대상: **국내 대표 반도체 기업 10개** (섹터: 반도체)
- 기능: 공시 수집 → AI 요약 → 목록/상세 조회 → 섹터별 필터
- 제외(후속 단계): 회원가입/알림, 다중 섹터, 모바일 앱, 유료화

> ⚠️ **면책 고지**: 본 서비스가 제공하는 요약과 해설은 투자 자문이 아니며, AI가 생성한 정보에는 오류가 있을 수 있다. 모든 판단의 근거는 반드시 DART 원문 공시로 확인해야 한다. (이 문구는 서비스 UI에도 상시 노출)

---

## 2. 대상 기업 (반도체 섹터 · MVP)

DART OpenAPI는 각 기업을 8자리 `corp_code`로 식별한다. 아래는 후보 목록이며, 실제 `corp_code`는 개발 시 `corpCode.xml`(전체 기업 고유번호 파일)에서 종목명·종목코드로 매칭하여 확정한다.

| # | 기업명 | 종목코드 | 서브 카테고리 |
|---|--------|----------|----------------|
| 1 | 삼성전자 | 005930 | 종합 반도체(IDM) |
| 2 | SK하이닉스 | 000660 | 메모리 |
| 3 | DB하이텍 | 000990 | 파운드리 |
| 4 | 한미반도체 | 042700 | 후공정 장비 |
| 5 | 리노공업 | 058470 | 테스트 부품 |
| 6 | 원익IPS | 240810 | 전공정 장비 |
| 7 | 주성엔지니어링 | 036930 | 전공정 장비 |
| 8 | 티씨케이(TCK) | 064760 | 소재 |
| 9 | 이오테크닉스 | 039030 | 레이저 장비 |
| 10 | 하나마이크론 | 067310 | 패키징(OSAT) |

> 목록은 확정본이 아니라 초안이다. 시가총액·거래량·공시 빈도 등을 기준으로 최종 10곳을 조정할 수 있다.

---

## 3. 기술 스택

| 계층 | 기술 | 비고 |
|------|------|------|
| 개발 방법론 | **Harness (Claude Code 에이전트 팀 팩토리)** | AI 에이전트 팀으로 개발 워크플로우 조율 (8 참조) |
| 백엔드 | **Django** + Django REST Framework | Python 기반, 관리자(admin)로 데이터 검수 용이 |
| 프론트엔드 | **HTML / CSS / JavaScript** | MVP는 Django 템플릿 기반 SSR, 필요 시 경량 JS |
| 데이터베이스 | **SQLite** (개발·운영 공통) | 공시·요약 저장. 선택 근거와 PostgreSQL 전환 트리거는 9.2 |
| 외부 API | **DART OpenAPI (OpenDART)** | 공시 원문 데이터 소스 |
| AI 요약 | **LLM API** (OpenAI API) | 공시 → 쉬운 요약 생성 |
| 배치 실행 | **cron** (6단계) → Celery + Celery Beat + Redis (7단계) | 월 약 45건 규모에서는 cron으로 충분. 적응형 폴링이 필요해지는 시점에 Celery 도입 (4.5) |
| 실시간 전달 (후속) | Django Channels(WebSocket) 또는 SSE + ASGI(Daphne/Uvicorn) | 브라우저로 신규 공시 즉시 푸시 (4.5) |
| 캐시·확장 | Redis 캐시 / CDN / DB 읽기 복제본 | 트래픽 확장 대응 (12) |
| 배포 | **Docker Compose + AWS Lightsail** (Ubuntu 24.04 LTS) | Caddy(HTTPS 자동 발급) · gunicorn · whitenoise. 구성과 기각 근거는 9.2 |

---

## 4. 외부 데이터 소스 — DART OpenAPI (OpenDART)

### 4.1 개요 및 인증

OpenDART는 금융감독원이 제공하는 공식 전자공시 OpenAPI다. 사용하려면 [인증키 신청](https://opendart.fss.or.kr/uss/umt/EgovMberInsertView.do)이 필요하며, 모든 요청 시 `crtfc_key` 파라미터에 발급받은 인증키를 담아 보낸다.

- **호출 제한**: 인증키 1개당 **일일 20,000회**(정책은 변동될 수 있으므로 [공식 개발가이드](https://opendart.fss.or.kr/guide/main.do?apiGrpCd=DS001)에서 재확인 필요). MVP 10개 기업 규모에서는 충분하나, 배치 스케줄링으로 여유 있게 운용한다.
- **응답 형식**: JSON / XML 선택 가능. 본 프로젝트는 JSON 사용.

### 4.2 사용할 주요 엔드포인트

| 용도 | 엔드포인트 | 핵심 파라미터 |
|------|-----------|----------------|
| 전체 기업 고유번호 | `/api/corpCode.xml` | `crtfc_key` (ZIP 다운로드, 종목명 → `corp_code` 매핑) |
| 공시 목록 조회 | `/api/list.json` | `corp_code`(선택), `bgn_de`, `end_de`, `pblntf_ty`, `page_no`, `page_count`(최대 100) |
| 기업 개황 | `/api/company.json` | `corp_code` |
| 공시서류 원본 | `/api/document.xml` | `rcept_no` (접수번호, 원문 XML) |
| 정기보고서 재무정보 | `/api/fnlttSinglAcnt.json` 등 | `corp_code`, `bsns_year`, `reprt_code` |

> 💡 **확장성 핵심**: `list.json`은 `corp_code` 없이 **날짜 범위만으로 시장 전체 공시**를 조회할 수 있다(페이지네이션). 이 특성이 12 확장 전략의 근간이 된다 — 기업을 기업별로 순회하지 않고 "그날 전체 공시를 몇 번의 호출로 받아 로컬에서 필터링"함으로써, 추적 기업 수가 늘어도 API 호출 수가 거의 늘지 않는다.

### 4.3 데이터 파이프라인 흐름

```
[1] corpCode.xml 다운로드 → 대상 10개 기업 corp_code 확정 (최초 1회 + 주기 갱신)
        ↓
[2] Celery 배치 (예: 30분 주기) → list.json 으로 신규 공시 목록 수집
        ↓
[3] 신규 rcept_no 발견 시 → document.xml 로 원문 본문 확보
        ↓
[4] 원문 전처리 (HTML/XML 정제, 표 텍스트화, 토큰 길이 관리)
        ↓
[5] LLM API 호출 → 쉬운 요약 + 핵심 포인트 + 중요도 태그 생성
        ↓
[6] DB 저장 (원문 링크 병기) → 웹에서 조회
```

### 4.4 신규 공시 자동 반영 (준실시간) — 실현 가능성 및 설계

**목표**: DART에 새 공시가 올라오면, 별도 조작 없이 우리 웹사이트에 AI가 정리한 공시 정보가 자동으로 나타나게 한다.

**실현 가능성**: ✅ 가능. 단, 방식에 전제가 있다.

OpenDART는 새 공시를 우리 서버로 밀어주는 **푸시(push)·웹훅(webhook) 기능을 제공하지 않는다.** 따라서 DART가 알려주기를 기다리는 대신, 우리가 공시 목록 API(`list.json`)를 **짧은 주기로 반복 조회(폴링, polling)** 하여 신규 공시를 스스로 감지하는 방식으로 구현한다. 이 때문에 엄밀히는 "실시간"이 아니라 **준실시간(near real-time)** 이며, DART 게시 후 폴링 주기(예: 5~10분) 이내에 반영된다.

**동작 흐름**:

```
[스케줄러] Celery Beat 가 5~10분마다 실행
        ↓
[감지] 대상 10개 기업에 대해 list.json 조회 (bgn_de = 최근 며칠)
        ↓
[비교] 응답의 rcept_no 를 DB의 기존 rcept_no 와 대조 → 신규만 선별
        ↓
[수집] 신규 공시의 원문(document.xml) 확보
        ↓
[요약] LLM 호출로 쉬운 요약 생성 (중요도 태그 부여)
        ↓
[저장] Disclosure + DisclosureSummary 저장
        ↓
[노출] 웹 피드에 자동 표시 (중요도 '높음'은 검수 후 게시 옵션)
```

**설계 시 고려사항**:

- **중복 방지**: `rcept_no`에 unique 제약을 두어, 폴링이 겹쳐도 같은 공시가 두 번 저장·요약되지 않게 한다(멱등성). 이미 처리한 공시는 LLM을 다시 호출하지 않으므로 비용도 아낀다.
- **폴링 주기 vs 비용**: 주기를 짧게 할수록 더 빠르게 반영되지만 API 호출·서버 부하가 늘어난다. MVP는 5~10분 권장(장 시간대만 단축하는 것도 가능).
- **호출 한도**: 10개 기업 × (24시간 / 폴링주기)로 계산해도 일일 호출 한도(약 20,000회) 대비 여유가 크다.
- **지연 시간 안내**: 완전한 실시간이 아님을 UI에서 "DART 게시 후 최대 N분 내 반영"으로 명확히 안내한다.
- **장애 대비**: 폴링 실패·요약 실패 시 재시도 큐를 두고, 놓친 구간은 다음 폴링에서 날짜 범위로 다시 훑어 보완한다. 단, **`corp_code` 없는 `list.json` 조회는 검색기간이 3개월로 제한된다**(오류 코드 100, 실측 경계 89일 통과 / 92일 실패). 공백이 3개월을 넘으면 한 번의 날짜 범위 조회로는 복구되지 않으므로, **89일 이하 창으로 분할해 순회**해야 한다. `poll_dart`가 `--bgn`/`--end`로 임의 구간을 받아 한도 초과 시 자동 분할하며(경계일 1일 중복 조회로 누락 차단), `rcept_no` unique 제약이 중복 조회를 무해하게 만든다.
- **(선택) 즉시성 강화**: 사용자가 특정 기업 페이지를 열 때 "지금 새로고침" 버튼으로 온디맨드 폴링을 트리거하면 체감 지연을 더 줄일 수 있다.

> 정리: 폴링 기반 준실시간 자동 반영은 기술적으로 무리 없이 구현 가능하며, 이미 4.3의 수집 파이프라인과 9 로드맵 1~2단계에 자연스럽게 포함된다. 신규 공시 감지 → 자동 요약 → 자동 노출까지 사람 개입 없이 흐르되, 중요도 높은 공시만 선택적으로 검수 게이트를 둔다.

### 4.5 준실시간화 심화 전략 (후속 단계)

MVP의 단순 폴링(4.4)을 넘어, 사용자가 **체감상 실시간**으로 느끼도록 지연을 더 줄이는 전략. DART는 푸시를 제공하지 않으므로 소스단 폴링은 유지하되, **감지·요약·전달** 세 지연을 각각 최적화한다.

**(1) 감지 지연 ↓ — 적응형 폴링 (Adaptive Polling)**

균일 주기 대신 DART 공시 접수 시간대(영업일 낮 시간대에 집중)에만 폴링을 촘촘히 한다.

- 접수 활동 시간대: 30초~1분 간격
- 야간·주말·공휴일: 10~30분 간격 또는 일시 중단

체감 지연이 분 단위 → 수십 초로 낮아지고, 야간 호출 낭비도 없앤다. (정확한 접수 운영 시간은 공식 가이드로 확인)

**(2) 전달 지연 ↓ — 브라우저로 실시간 푸시 (WebSocket/SSE)**

감지는 폴링이지만, **감지 순간 사용자 화면으로는 실시간 푸시**한다. 새로고침 없이 새 카드가 나타난다. Django Channels(WebSocket) 또는 더 단순한 SSE로 구현하며, ASGI 서버(Daphne/Uvicorn)가 필요하다.

**(3) 요약 지연 ↓ — 낙관적 렌더링 (Optimistic Render)**

LLM 요약은 수 초 걸리므로, 감지 즉시 **원문 메타데이터(제목·기업·시각)를 먼저 노출**하고 요약 자리는 "AI가 정리 중…" 상태로 띄운 뒤, 요약 완성 시 그 부분만 채운다(스트리밍 가능).

**통합 코드 레벨 파이프라인**:

```
Celery Beat (적응형 주기)
   └─ poll_dart(): list.json 조회 → 신규 rcept_no 선별
         └─ 신규 발견 시 각각 Celery 태스크 enqueue
               ├─ (a) Disclosure 저장 → 즉시 Channels group_send ──▶ 브라우저에 "새 공시" 카드 push (요약 pending)
               └─ (b) LLM 요약 생성 → DisclosureSummary 저장 → 다시 group_send ──▶ 카드의 요약 부분 채움
```

```python
# tasks.py (Celery) — 개념 스케치
@shared_task
def poll_dart():
    # corp_code 없이 날짜 범위로 시장 전체 공시를 받아 로컬 필터링 (12 확장 전략)
    tracked = set(Company.objects.filter(is_active=True)
                          .values_list("corp_code", flat=True))
    for item in dart.list(bgn_de=recent, end_de=today):   # 페이지네이션 순회
        if item["corp_code"] not in tracked:
            continue
        d, created = Disclosure.objects.get_or_create(
            rcept_no=item["rcept_no"], defaults=...)        # unique → 멱등
        if created:
            push_to_browser(d, status="new")                # ① 즉시 카드 노출
            summarize_disclosure.delay(d.id)                # ② 요약은 비동기

@shared_task
def summarize_disclosure(disclosure_id):
    d = Disclosure.objects.get(id=disclosure_id)
    summary = llm_summarize(fetch_document(d.rcept_no))
    DisclosureSummary.objects.create(disclosure=d, **summary)
    push_to_browser(d, status="summarized")                 # ③ 요약 채움
```

**트레이드오프**: 이 구성은 Celery/Redis에 더해 Channels(또는 SSE)·ASGI가 필요해 복잡도가 오른다. 따라서 **MVP는 단순 폴링 + 새로고침으로 시작(4.4) → 후속 단계에서 적응형 폴링 + 푸시 + 낙관적 렌더링을 얹는** 순서를 권장한다.

---

## 5. AI 요약 파이프라인 설계

### 5.1 요약 산출물 구조

각 공시에 대해 LLM이 아래 구조화된 결과를 생성한다.

- **한 줄 요약**: 공시의 핵심을 한 문장으로 (예: "설비 투자를 위해 3천억 원 규모 회사채를 발행합니다.")
- **쉬운 설명**: 3~5문장, 전문 용어를 풀어서 해설
- **왜 중요한가**: 일반 투자자 관점에서의 의미
- **중요도 태그**: 높음 / 보통 / 낮음 (예: 공급계약, 유상증자 = 높음)
- **공시 유형 분류**: 정기공시, 주요사항보고, 지분공시 등

### 5.2 프롬프트 전략

- 시스템 프롬프트로 "회계 비전공 일반인 대상", "과장·투자 권유 금지", "불확실한 내용은 단정하지 말 것"을 명시
- 원문에 없는 사실을 지어내지 않도록(할루시네이션 억제) **원문 인용 근거**를 함께 요구
- 출력은 JSON 스키마로 강제하여 파싱 안정성 확보

**역할 분담 원칙 — 산술은 코드가, 판단은 LLM이 (5단계에서 확립).**

한국어 수 단위(만·억·조)는 4자리로 끊고 콤마는 3자리로 끊는다. 이 어긋남 때문에 LLM이
콤마를 기준으로 단위를 갈라 **정확히 10배 틀린 값**을 반복해서 냈다. 원문 숫자 자체는
한 자리도 틀리지 않았다 — 읽기가 아니라 계산에서 틀렸다.

- **결정적 계산(단위 환산·표기)은 코드가 한다** — `units.py`의 순수 함수. 오답률 0.
- **LLM은 원문 숫자를 그대로 옮겨 적기만 한다** — 금액은 반드시 `원`으로 끝낸다.
- 프롬프트의 실제 역할은 정답을 내는 것이 아니라 **검증 가능한 출력**(인용 근거·단위
  접미사)을 만드는 것이다. 검증기가 성립하는 근거가 여기에 있다.

### 5.3 품질·정확성 관리 (중요)

공시 요약은 오류 시 사용자 판단에 악영향을 줄 수 있으므로, 정확성 검증 장치를 둔다.

- **원문 링크 상시 병기**: 모든 요약 하단에 DART 원문 URL 노출
- **검수 큐**: **공시 유형**을 기준으로 Django admin에서 사람이 확인 후 게시한다.
  초안은 AI가 매긴 중요도('높음')를 게이트로 삼았으나, 검증 대상인 AI의 판단을 게이트로
  쓰는 것은 순환이라 5단계에서 공시 유형 기준으로 옮겼다(`review_policy.py`).
  기준은 "요약이 틀렸을 때 독자가 치르는 대가가 되돌릴 수 없는가".
- **숫자 검증**: 금액·비율 등 수치는 원문에서 추출한 값과 대조하는 후처리 로직
  (`verification.py`). 원문이 선언한 단위(`단위 : 백만원`)만 배수로 인정한다.
- **자동 미게시**: 수치 검증에 실패하면 사람 손을 기다리지 않고 즉시 웹에서 감춘다.
  **삭제가 아니라 감추기**이므로 재수집해도 LLM을 다시 부르지 않는다(11 참조).
- **면책 문구 상시 노출**

---

## 6. 데이터베이스 스키마 (초안)

```
Sector (섹터)
  - id
  - name              # 예: "반도체"
  - slug
  - description

Company (기업)
  - id
  - sector            # FK → Sector
  - corp_code         # DART 8자리 고유번호 (unique)
  - stock_code        # 종목코드 6자리
  - name
  - sub_category      # 예: "메모리", "파운드리"
  - is_active

Disclosure (공시 원문)
  - id
  - company           # FK → Company
  - rcept_no          # DART 접수번호 (unique)
  - report_name       # 공시 제목
  - disclosure_type   # 공시 유형
  - filed_at          # 접수일자
  - dart_url          # 원문 링크
  - raw_fetched       # 원문 확보 여부

DisclosureSummary (AI 요약)
  - id
  - disclosure        # OneToOne → Disclosure
  - one_line          # 한 줄 요약
  - easy_explanation  # 쉬운 설명
  - why_important     # 왜 중요한가
  - importance        # 높음/보통/낮음
  - model_name        # 사용 LLM 버전 (재현성)
  - is_reviewed       # 사람 검수 여부
  - created_at
```

---

## 7. 화면 설계 (초안)

| 화면 | 설명 | 주요 요소 |
|------|------|-----------|
| **메인 / 섹터 목록** | 섹터 카드 그리드 | 반도체 섹터 진입, 최신 주요 공시 하이라이트 |
| **섹터 상세** | 반도체 기업 10곳의 최신 공시 통합 피드 | 기업 필터, 중요도 필터, 날짜 정렬 |
| **기업 상세** | 특정 기업의 공시 타임라인 | 기업 개황, 공시 카드 리스트 |
| **공시 상세** | 개별 공시의 AI 요약 전문 | 한 줄 요약 → 쉬운 설명 → 왜 중요한가 → 원문 링크 |

카드 UI 예시(공시 상세):

```
┌─────────────────────────────────────────────┐
│ [중요도: 높음]  삼성전자 · 2026-07-22        │
│ 단일판매·공급계약 체결                        │
├─────────────────────────────────────────────┤
│ 📌 한 줄 요약                                 │
│   대형 고객과 2조 원 규모 반도체 공급         │
│   계약을 맺었습니다.                          │
│                                               │
│ 💬 쉬운 설명                                  │
│   ... (3~5문장) ...                           │
│                                               │
│ ❓ 왜 중요한가                                │
│   ... 매출에 미치는 영향 ...                  │
│                                               │
│ 🔗 DART 원문 보기                             │
└─────────────────────────────────────────────┘
```

---

## 8. Harness 기반 개발 워크플로우

이 프로젝트는 [Harness](https://github.com/revfactory/harness) — Claude Code용 팀 아키텍처 팩토리 — 를 개발 방법론으로 활용한다. Harness는 도메인 설명을 입력하면 전문 에이전트 팀(`.claude/agents/`)과 스킬(`.claude/skills/`)을 자동 생성해, 복잡한 개발 작업을 역할별로 분해·조율한다.

본 프로젝트에 적합한 **파이프라인 패턴** 에이전트 팀(안):

- **Analyst 에이전트**: DART API 스펙 분석, 공시 유형별 데이터 구조 파악
- **Backend 에이전트**: Django 모델·API·Celery 배치 구현
- **AI/Prompt 에이전트**: LLM 요약 프롬프트 설계 및 출력 스키마 관리
- **Frontend 에이전트**: 템플릿·화면·카드 UI 구현
- **QA 에이전트**: 요약 정확성 검증, 숫자 대조, 테스트

개발 초기에 `하네스 구성해줘` 프롬프트로 위 팀을 스캐폴딩하고, 진행하며 `/harness:evolve`로 실제 사용된 아키텍처를 팩토리에 되먹여 개선한다.

> 참고: Harness는 실험적 에이전트 팀 기능(`CLAUDE_CODE_EXPERIMENTAL_AGENT_TEAMS=1`)을 요구하며, 공식 런타임은 Claude Code 기준이다.

---

## 9. 개발 로드맵 (단계별)

| 단계 | 목표 | 주요 산출물 | 상태 |
|------|------|-------------|------|
| **0. 셋업** | 저장소·Django 프로젝트 초기화, Harness 팀 구성 | 개발 환경, 에이전트 팀 | 완료 |
| **1. 데이터 수집** | DART 인증키 발급, corpCode 매핑, 공시 수집 배치 | `Disclosure` 적재 파이프라인 | 완료 |
| **2. AI 요약** | LLM 요약 파이프라인 + 출력 스키마 | `DisclosureSummary` 생성 | 완료 |
| **3. 웹 조회** | 섹터/기업/공시 상세 화면 | 동작하는 MVP 웹 | 완료 |
| **4. 품질·검증** | 숫자 대조, 면책 문구, 테스트, 사람 검수 워크플로우 | admin 검수 큐 | 완료 (PR #9) |
| **5. 요약 자동 교정** | 단위 환산을 코드로 이관, 검증 실패 → 자동 미게시 | `units.py`·`verification.py`·`review_policy.py` | 완료 (PR #15) |
| **6. 배포** | Lightsail + Docker로 공개 서비스 구축, 파이프라인 무인 자동화 | HTTPS 공개 URL · cron 스케줄 · S3 백업 · `docs/RUNBOOK.md` | 완료 (PR #17) |
| **7. 준실시간화 (후속)** | 적응형 폴링 + WebSocket/SSE 푸시 + 낙관적 렌더링 (4.5) | 체감 실시간 반영 | 미착수 |
| **8. 확장 (후속)** | 날짜별 일괄 폴링 전환, 캐시·CDN·읽기 복제본, 다중 섹터 (12) | 트래픽·커버리지 확장 | 미착수 |

### 9.1 로드맵 변경 이력

초안에서는 **5. 배포**가 4단계 바로 뒤에 왔다. 실행 과정에서 두 가지가 달라졌고,
아래는 그 기록이다.

**(1) 4단계가 둘로 갈라졌다 — 5단계 신설** *(2026-08-21 기록)*

4단계에서 검수 화면을 만들고 나서야 근본 원인이 드러났다. 요약 오류의 원인은
"AI가 원문을 잘못 읽어서"가 아니라 **단위 환산이라는 산술을 LLM에게 맡겼기 때문**이었다
(5.2 참조). 사람이 손으로 고치는 검수는 이 원인을 줄이지 못하고 건수만 흡수한다.

그래서 산술을 코드로 옮기고 검증 실패를 자동 미게시로 연결하는 작업을 별도 단계로 세웠다.
커밋(`7c65da4`)·테스트·브랜치가 이미 이 작업을 "5단계"로 부르고 있어 문서를 실제 번호에
맞췄다. **배포는 5 → 6으로, 이후 단계도 하나씩 밀린다.**

**(2) 배포 앞에 요약 품질 안정화를 넣는다** *(2026-08-21 결정)*

5단계 본체가 끝난 시점에 남은 문제가 셋이다.

| 항목 | 내용 |
|---|---|
| v2 잔재 | 요약 140건 중 113건이 옛 프롬프트 산출물. **검증기가 못 잡은 10배 오류가 실제로 1건 있었다**(pk 117) |
| 검증기 | 조(10¹²)만 닫았고 10³~10⁸은 여전히 무조건 허용 — 천 배 오류가 통과한다 |
| 비용 추정 | `FIXED_PREFIX_OVERHEAD_TOKENS`가 v2 실측값이라 실제보다 1.5~1.8배 낮게 나온다 |

검증기 전면 좁히기는 **v2 잔재를 걷어낸 뒤에야 측정이 유효하다** — 지금 재면 새로 생기는
오탐 25건의 대부분이 v2에서 나와 잘못된 결론에 이른다. 따라서
**전량 재요약 → 검증기 보완 → 배포** 순으로 진행한다.
공개 서비스에 검증기가 못 잡는 오류를 실어 보내지 않기 위한 순서다.

**(3) 그 순서가 완결됐다 — 6단계 착수** *(2026-08-26 기록)*

전량 재요약(PR #13)과 게시 게이트 분리(PR #15)를 마쳤다. 요약 140건이 전부 v3 프롬프트
산출물이고, 자동 미게시 0건 · 자동 검증 경고 22건 · 테스트 333건 통과다. 위 (2)가 배포의
선행 조건으로 걸어둔 항목이 모두 해소되어 6단계를 시작한다.

남은 요약 품질 과제(`evidence` maxItems 8 → 12, 인용 중복 금지)는 재요약 비용 약 $3을
다시 요구하므로 **배포 이후로 미룬다**. 지금 서비스에 실리는 요약의 정확성을 막는 문제가
아니라, 검수자가 보는 근거 인용의 충실도 문제다.

**(4) 6단계를 닫으면서 재구축 리허설을 범위에서 뺐다** *(2026-08-29 결정)*

이슈 #16의 완료 조건에는 "저장소와 S3 백업만으로 서버를 처음부터 다시 세워 본다"가
있었다. 나머지 조건(공개 URL·HTTPS·cron 무인 완주·백업 무결성)이 모두 충족된 시점에
이 항목만 남았고, 단계를 더 끌기보다 닫는 쪽을 택했다.

**따라서 복원 절차는 "문서화됨"이지 "검증됨"이 아니다.** `deploy/restore.sh`와
RUNBOOK 6장은 있으나 전 구간을 실제로 밟아본 적이 없다. 백업 파일 자체의 무결성은
확인했지만(`PRAGMA integrity_check` + 건수 대조), 빈 서버에서 시작해 서비스가 다시
뜨는 데까지는 미검증 구간이다. 장애가 난 다음에야 처음 실행해 보는 스크립트가 되는
셈이라, 남은 리스크로 13장에 명시해 둔다.

### 9.2 6단계 배포 구성 결정 기록

이 프로젝트의 **실측값**에 근거해 정했다. 일반적인 웹 서비스 관례와 다른 선택이 여럿이라
근거를 남긴다.

| 실측 | 값 | 측정 방법 |
|---|---|---|
| 웹 요청 경로 메모리 | **45MB** | `views`·`models`·`verification`만 import한 프로세스 RSS |
| 요약 프로세스 피크 | 157MB | 위 + tiktoken(o200k_base) + openai SDK + 최대 원문 토큰화 |
| 동시 쓰기 주체 | **1** | `views.py`에 쓰기 코드 0줄 — 수집·요약(cron)과 admin 검수만 쓴다 |
| 데이터 규모 | 1,114행 / 10.7MB | Sector 1 · Company 10 · Disclosure 963 · Summary 140 |
| 월 요약 대상 | 약 45건 | 05월 46 · 06월 39 · 07월 43 |
| 요약 1건당 비용 | $0.023 | 전량 재요약 실측 ($3.2329 ÷ 140) |

**확정 구성**

| 계층 | 선택 |
|---|---|
| 서버 | AWS Lightsail 1GB · 2vCPU · SSD 40GB · 전송 2TB · 서울(ap-northeast-2) |
| OS | Ubuntu 24.04 LTS |
| 컨테이너 | Docker Compose (`python:3.11-slim` · `linux/amd64`) |
| DB | SQLite (WAL 모드) — 인스턴스 내 파일, 볼륨 마운트 |
| 웹 서버 | Caddy — Let's Encrypt 자동 발급·갱신, `/admin/*` IP 제한 |
| WSGI | gunicorn `--workers 2` |
| 정적 파일 | whitenoise |
| 스케줄러 | 호스트 cron |
| 백업 | 일 1회 SQLite → S3 (30일 수명주기) · Lightsail 자동 스냅샷 일 1회(7개 보관) |

메모리 예산 상시 약 445MB / 요약 피크 약 602MB (1,024MB의 59%). 월 비용 약 $8.5.

> **실측 (2026-08-27, 배포 직후 유휴):** 호스트 전체 549MB 사용 / 911MB 가용
> (1,024MB 중 커널 예약분 제외). 컨테이너는 web 113MB + Caddy 26MB = 139MB로
> 예상대로였고, **초과분 약 100MB는 호스트 쪽**(Docker 데몬·snapd·systemd)이다.
> 요약 피크(+157MB)를 얹으면 약 706MB — 가용 메모리의 78%다. 여유는 있지만
> 예산보다 빡빡하므로 30분마다 `free`를 남기는 cron으로 추이를 지켜본다.

> **실측 (2026-08-28, cron 무인 완주 1회차):** 07:00 수집 → 07:10 선별 → 07:20 원문 →
> 07:30 요약이 사람 손 없이 완주했다. 요약 3건 생성에 **$0.0274**(건당 $0.0091)로
> 예산 $0.023의 40% 수준이다 — 프롬프트 캐시가 56% 적중한 덕이다. 호스트 메모리는
> **491~501MB 사용 / 409~420MB 가용**으로, 전날 549MB보다 오히려 안정됐다.
>
> **다만 요약 피크는 측정하지 못했다.** 샘플러가 07:30:01에 찍는데 요약 3건은 1분 안에
> 끝나므로, 30분 간격으로는 짧은 배치의 피크를 구조적으로 놓친다. 706MB는 여전히
> 추정치이며, 요약 창(07:30~07:33)에서만 10초 간격으로 찍어 확인한다.

> 설계 시점에는 스냅샷을 주 1회($0.25)로 잡았으나, Lightsail 자동 스냅샷은 주기를
> 고를 수 없고 **일 1회·7개 보관 고정**이다(2026-08-27 콘솔 확인). 수동 주간으로
> 낮출 수 있지만 사람이 기억해야 하는 일은 결국 안 하게 되므로 자동 일간을 택했다.
> 차액은 월 $0.2 남짓이다.

**채택하지 않은 것**

| 후보 | 기각 근거 |
|---|---|
| EC2 | 동일 1GB에 $11.6 vs $7. EC2는 공인 IPv4·EBS·전송이 별도 청구된다. 스냅샷 → EC2 내보내기가 공식 지원되므로 7단계에 이전 가능 |
| PostgreSQL / MySQL | 동시 쓰기 1 · 데이터 11MB · 웹은 읽기 전용. SQLite 종속 코드 0건 확인(원시 SQL 0 · 대소문자 의존 쿼리 0 · `USE_TZ=True`) → 필요해지면 반나절에 전환 |
| RDS · 관리형 DB · 별도 DB 인스턴스 | 월 $8 → $23~27. 그 돈이 막아주는 손실이 "$3와 반나절"이다 — 원본은 DART에 있고 요약은 재생성 가능하다 |
| Celery + Redis | 월 45건에 메시지 브로커는 과설계. 4.5가 권고한 "단순 폴링으로 시작 → 후속 단계에서 얹기" 순서를 따른다 |
| S3 정적 호스팅 · CDN · 로드밸런서 | CSS 2개 + JS 1개, 서버 1대 |
| nginx + certbot | Caddy가 HTTPS를 자동 처리한다 (설정 30줄 → 5줄) |

**PostgreSQL 전환 트리거** — 아래 중 하나라도 발생하면 전환한다.

1. **Celery 워커를 별도 프로세스·인스턴스로 분리**할 때 (7단계, 12.3) — 쓰기 주체가 2개 이상이 된다
2. **웹 서버를 2대 이상**으로 늘릴 때 — 파일 DB는 인스턴스 간 공유가 불가능하다
3. **웹에서 쓰기 기능**을 추가할 때 (계정·댓글·알림 구독)
4. **공시 검색 기능**을 추가할 때 — 한국어 전문 검색은 PostgreSQL이 유리하다

즉 SQLite는 "지금이 1인·읽기 전용·11MB이기 때문에" 맞는 선택이지, 영구 결정이 아니다.
위 조건 중 하나에 닿는 순간이 전환 시점이다.

---

## 10. 리스크 및 고려사항

- **정확성 리스크**: AI 요약 오류 → 원문 링크 병기, 사람 검수, 면책 문구로 완화
- **법적/약관**: 10.1에서 확인 완료
- **API 호출 제한**: 일일 한도 대응은 12에서 상세 설계(날짜별 일괄 폴링, 기관 키, 적응형 폴링)
- **LLM 비용**: 11 비용 구조에서 상세 관리(요약 캐싱·재사용, 중요 공시 우선)
- **데이터 품질**: 원문 표·이미지 기반 공시의 전처리 난이도

### 10.1 OpenDART 이용약관 검토 (2026-08-27)

6단계 공개 배포는 DART 데이터의 재배포에 해당하므로 [이용약관](https://opendart.fss.or.kr/intro/terms.do)을
확인했다. **결론: 이 서비스 형태에 금지 조항은 없다.** 다만 명시적 허용도 없다.

| 조항 | 내용 | 우리에게 미치는 영향 |
|---|---|---|
| 제11조 | 서비스는 원칙적으로 무료 | 비용 없음 |
| 제16조 | **오픈API 서비스 및 관련 프로그램**의 저작권은 금융감독원 | API·프로그램에 대한 것이지 공시 데이터 자체가 아니다 |
| 제19조 | **인증키를 제3자가 이용하게 할 수 없다.** 관리 소홀로 인한 손해는 회원 부담 | 유일하게 명확한 의무. 키는 서버 `.env`에만 두고 응답·클라이언트로 절대 내보내지 않는다 |
| 제23조 | 공시정보는 **공시제출인의 책임 하에 작성**되며 금융감독원은 정확성·완전성을 보장하지 않는다 | 원본부터 무보증이다. 우리는 거기에 AI 가공을 한 겹 더 얹으므로 면책이 이중으로 필요하다 |

**약관에 없는 것** — 재배포 가능 여부, 출처표시 의무, 상업적 이용 제한에 관한 조항이
아예 없다. 약관에 정하지 않은 저작권 사항은 저작권법과 공공데이터법을 따른다.
OpenDART 사이트에는 **공공누리(KOGL) 표시가 없고** 푸터가
`Copyright (c) financial supervisory service. all rights reserved.`다.
즉 자유이용이 선언되어 있지 않으므로 **보수적으로 처리**한다.

**적용한 조치**

1. 출처를 푸터에 명시하고 DART로 링크한다
2. **금융감독원과 무관한 개인 프로젝트임을 상시 고지한다** — 출처만 적으면 공식
   서비스로 오인될 수 있다. 오인이야말로 실질적인 위험이다
3. 원본 무보증(제23조)의 취지를 면책 문구에 반영한다
4. 요약이 보이는 모든 화면에 원문 링크를 병기한다 (기존 정책, 1.4·5.3)
5. 인증키는 서버 환경변수에만 둔다 (제19조)

**다시 검토가 필요한 시점** — 유료화하거나, 요약 데이터를 API로 제3자에게 제공하거나,
DART 원문 자체를 그대로 재배포할 때. 지금은 셋 다 해당하지 않는다.
법률 자문을 받은 것이 아니라 약관을 읽고 보수적으로 판단한 결과다.

---

## 11. 비용 구조

서비스의 비용은 데이터 소스가 아니라 **가공·운영 계층**에서 발생한다.

| 항목 | 과금 방식 | 규모/특징 |
|------|-----------|-----------|
| **DART OpenAPI** | **무료** | 호출 자체는 무료. 돈이 아니라 **일일 호출 횟수(약 20,000회)로만 제한**. 초과 시 과금이 아니라 당일 차단 → 대응은 12. |
| **LLM API** | **토큰 종량제** (변동비) | 사실상 유일한 유의미 변동 비용. 공시 원문 길이에 비례. 긴 사업보고서가 비용 큼. |
| **인프라** | 사용량 기반 (준고정비) | AWS EC2/RDS/Redis 등 서버·DB 운영비. 트래픽에 따라 증설. |

**LLM 비용 관리 원칙 (핵심 변동비)**:

- **요약 1회 생성 후 DB 저장·영구 재사용**: 같은 공시를 사용자가 몇 번 열든 LLM은 딱 한 번만 호출된다. `rcept_no` unique 제약(멱등성)이 중복 요약을 원천 차단한다.
- **중요도 기반 차등 처리**: 중요도 '낮음' 공시는 짧은 요약 또는 지연 처리, '높음'만 상세 요약해 토큰을 아낀다.
- **전처리로 토큰 절감**: 원문에서 표·반복 문구·불필요 섹션을 정제해 입력 토큰을 줄인다.
- **모델 티어링**: 단순 분류·중요도 판정은 저비용 소형 모델, 최종 해설만 고성능 모델로 분리 가능.

> 요점: **데이터 수집(DART)은 공짜**이고, 비용의 핵심은 LLM 호출을 얼마나 아끼느냐다. "한 공시당 한 번만 요약"이 비용 통제의 근간이다.

---

## 12. 확장성 및 API 한도 대응

### 12.1 핵심 통찰 — 사용자 트래픽 ≠ DART 호출

사용자는 우리 **DB/캐시**에서 읽고 DART를 직접 호출하지 않는다. DART를 호출하는 것은 백엔드 수집 파이프라인뿐이므로:

> **DART 호출 수 ≈ (추적 기업 수 × 폴링 빈도) — 사용자 수와 무관**

사용자가 10명이든 100만 명이든 DART 호출은 동일하다. 따라서 트래픽 급증은 DART 한도 문제가 아니라 **우리 웹/DB의 확장성 문제**(12.3)이고, DART 한도가 늘어나는 진짜 축은 **커버리지 확장(추적 기업 증가)**(12.2)이다.

### 12.2 API 호출 한도 대응 (커버리지 확장 시)

**① 날짜별 일괄 폴링 (가장 강력)**

`list.json`을 `corp_code` 없이 **날짜 범위만으로 호출**하면 그날 시장 전체 공시를 페이지네이션으로 받는다. 이를 로컬에서 추적 기업만 필터링한다.

- 효과: 추적 기업이 10곳이든 1,000곳이든 **호출 수가 거의 불변**. 기업 수와 호출 수의 결합이 끊긴다.
- 개략 계산: 활동 12시간 × 1분 간격 × 페이지 약 3회 ≈ 하루 **약 2,000여 회** → 2만 한도 대비 큰 여유로 사실상 전 종목 커버.
- **전제 — 한 번에 조회할 수 있는 기간은 3개월**: `corp_code`를 빼는 대가로 검색기간 제한이 걸린다(오류 코드 100 "corp_code가 없는 경우 검색기간은 3개월만 가능합니다", 실측 경계 89일 통과 / 92일 실패). 5~10분 주기의 정상 폴링은 며칠 범위만 훑으므로 이 한도에 닿지 않아 **전략은 그대로 유효하다.**
- **예외는 백필·장애 복구뿐**: 과거 데이터를 한 번에 채우거나 3개월 넘는 공백을 메울 때만 한도에 걸린다. 이때는 89일 이하 창으로 분할 순회한다(4.4). 분할하면 호출 수가 창 수만큼 늘어나므로, 긴 백필은 일일 한도를 감안해 여러 날에 나눠 실행한다(`poll_dart`가 실행 전 예상 호출량을 경고).

**② 기관용 인증키 (unlimited 혜택)**

인증키를 **기관(institutional)**으로 신청하면 폴링의 대부분을 차지하는 **공시목록·기업개황 조회가 무제한**으로 열린다(공식 가이드 재확인 권장). 폴링 한도 부담이 크게 완화된다.

**③ 적응형 폴링 + 캐싱·멱등성 (4.5)**

야간·주말 폴링 축소, `rcept_no` unique로 중복 원문·요약 호출 차단.

**④ 최후의 보험 — 키 분리·레이트 리미팅**

극단적 규모에서는 수집기를 여러 인증키로 분산하거나, 원문 상세 호출을 큐에 쌓아 한도 내에서 평탄화한다. ①②를 적용하면 여기까지 갈 일은 거의 없다.

### 12.3 트래픽(사용자) 확장 대응 — DART와 별개

사용자 트래픽 증가는 웹/DB 계층에서 독립적으로 해결한다.

- **읽기 캐싱**: 공시 목록·상세는 거의 불변이므로 Redis 캐시 / CDN으로 대부분의 읽기를 흡수.
- **정적/SSR 캐시**: 완성된 요약 페이지는 사실상 정적 → 페이지 캐시 적중률이 높다.
- **DB 확장**: 읽기 복제본(read replica)으로 조회 부하 분산, 인덱스(기업·날짜·중요도) 최적화.
- **웹 서버 수평 확장**: 무상태(stateless) 앱 서버를 로드밸런서 뒤에서 오토스케일.
- **수집/전달 분리**: 수집(Celery)과 웹 서빙을 별도 프로세스·인스턴스로 분리해 서로 영향 없게 한다.

### 12.4 확장 단계 요약

| 확장 축 | 병목 | 해결 계층 | 핵심 수단 |
|---------|------|-----------|-----------|
| 사용자 증가 | 웹/DB | 서빙 계층 | 캐시·CDN·읽기 복제본·오토스케일 |
| 커버리지(기업) 증가 | DART 한도 | 수집 계층 | 날짜별 일괄 폴링·기관 키·적응형 폴링 |
| 실시간성 요구 | 지연 | 수집+전달 | 적응형 폴링·WebSocket 푸시·낙관적 렌더링 |

---

## 13. 다음 액션 아이템

*(2026-08-29 갱신. 6단계에서 끝난 항목(배포 실행·법적 검토)과 근거 인용 보강(→ 13.1)은
목록에서 내렸다. 끝나지 않은 채 범위에서 빠진 항목은 지우지 않고 아래 "남은 리스크"로 옮겼다.)*

1. **운영 관찰** — 배포 직후라 실측이 하루치뿐이다. 아래 둘을 며칠 지켜본다.
   - **메모리 피크** — 30분 샘플러로는 1분 안에 끝나는 요약 배치를 못 잡아 706MB는
     여전히 추정치다. 배치 창(07:30~07:33)만 10초 간격으로 찍는 cron을 넣어 뒀다.
   - **`fetch_documents` 무한 재시도** — 원문이 아직 안 올라온 공시(DART `[014]`)를
     매일 다시 부른다. 영영 안 올라오는 유형이 있다면 재시도 상한이 필요하다.
2. **7단계 준실시간화** — 적응형 폴링 + WebSocket/SSE 푸시 + 낙관적 렌더링 (4.5).
   착수하면 **인프라 전제가 바뀐다**: 6단계가 SQLite·cron·WSGI를 고른 근거는
   "쓰기 주체가 하나"였는데(9.2), 이 단계에서 Celery worker가 여럿이 되고 ASGI 상시
   연결이 붙는다. Celery·Redis·PostgreSQL 전환과 인스턴스 상향을 함께 판단해야 한다.
   폴링 주기는 1번의 관찰 결과가 나온 뒤에 정한다.
3. (후속) 날짜별 일괄 폴링, 다중 섹터 확장 (12장)

**남은 리스크 (해소되지 않은 채 범위에서 빠진 것)**

- **재구축 리허설 미실시** — 복원 절차는 문서화됐으나 전 구간을 실행해 본 적이 없다.
  장애 시 처음 돌려 보는 스크립트가 된다. 경위는 9.1-(4).
- **중복 인용이 남아 있다** — 프롬프트 지시만으로는 65% → 58%에 그쳤다. 아래 13.1 참조.
- **"원문에서 찾지 못한 인용"이 늘었다** — 7.9% → 11.0%. 근거를 더 쓰게 한 대가다.

### 13.1 근거 인용 보강 결과 (2026-08-29, 이슈 #19 · PR #20)

`evidence` 상한을 8 → 12로 올리고 중복 인용 금지를 넣어 전량 재요약했다(v3 → v4).
**상한이 병목이라는 가설은 맞았고, 중복 금지는 사실상 실패했다.**

| 지표 | v3 (140건) | v4 (146건) | |
|---|---|---|---|
| 8칸에서 멈춘 요약 | 101건 (72%) | — | 상한 해소 |
| 9칸 이상을 쓴 요약 | 0건 | **30건 (21%)** | 예전 같으면 못 적었을 근거 |
| 인용에 없는 수치 | 10건 (7.1%) | **4건 (2.7%)** | **−62%** |
| 원문에서 찾지 못한 인용 | 11건 (7.9%) | 16건 (11.0%) | +39% |
| 중복이 있는 요약 | 91건 (65%) | 84건 (58%) | −7%p |
| 비용 | — | **$3.3591** | 건당 $0.0230 |

**(1) 상한 8은 실제로 병목이었다.** 요약의 21%가 8칸을 넘겨 쓴다. 겨냥한 경고
("인용에 없는 수치")가 62% 줄었다 — 요약이 틀려서가 아니라 근거를 적을 자리가
없어서 붙던 오탐이었음이 확인됐다.

**(2) 중복 금지를 LLM에게 시킨 것은 실패였다.** 프롬프트에 금지 지침을 넣었으나
65% → 58%에 그쳤다. **이것은 v3에서 이미 배운 교훈의 재현이다** — 단위 환산을 LLM에게
맡겼다가 10배 오류를 낸 뒤 "기계적으로 판정 가능한 일은 코드가 한다"로 옮겼는데(5.2),
중복 제거도 정확히 같은 부류다. 판정 코드(`verification._duplicate_indexes`)는 이미
정확히 동작하므로, 다음 프롬프트 개정 때 **코드로 제거**한다. 지금 $3을 다시 쓰지 않는
이유는 중복이 사실 오류도 오탐도 아니고, 검수 화면에 배지로 이미 보이기 때문이다.

**(3) 근거를 더 쓰게 하면 인용 정확도가 떨어진다.** "원문에서 찾지 못한 인용"이
11 → 16건으로 늘었다. 근거 수가 늘면 표에서 셀을 재조합한 인용이 나올 기회도 함께
는다. 총 경고가 22 → 19건(16% → 13%)에 그친 이유가 여기다. 이 유형은 상한과 무관한
별개 결함이므로 다음에 따로 다룬다.

**(4) 비용 예측이 확정됐다.** 비용 모델이 계산한 $3.3596과 실제 청구 $3.3591의 오차가
0.0%였고, 정적 접두사 4,017토큰도 146회 호출 전부에서 확인됐다. 다만 **작은 배치의
단가를 큰 배치에 적용하면 안 된다** — 일일 3건 배치는 캐시 적중 56%에 건당 $0.0091인데,
146건 일괄은 적중 36%에 건당 $0.0230이었다. 공시 원문이 저마다 달라 재활용되는 것이
고정 접두사뿐이기 때문이다. 사전 추정 $1.3~3은 이 차이를 놓쳐 낮게 잡은 값이었다.

---

## 참고 자료

- 전자공시 OPENDART 시스템: https://opendart.fss.or.kr/
- OpenDART 오픈API 소개: https://opendart.fss.or.kr/intro/main.do
- OpenDART 개발가이드(공시정보): https://opendart.fss.or.kr/guide/main.do?apiGrpCd=DS001
- OpenDART 인증키 신청: https://opendart.fss.or.kr/uss/umt/EgovMberInsertView.do
- Harness (Claude Code 팀 아키텍처 팩토리): https://github.com/revfactory/harness
